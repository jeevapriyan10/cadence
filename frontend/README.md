# Cadence Frontend

The web client for **Cadence** — a railway block-window scheduling engine. Built with React, TypeScript, and Vite.

## Development

```bash
# Install dependencies
npm install

# Run development server
npm run dev

# Run unit / smoke tests
npm test

# Production build
npm run build
```
